---
name: docstrings
description: Add documentation to public APIs
---

Use the `envoy:docstrings` skill exactly as written.

This adds:
- C#: XML documentation (`/// <summary>`)
- TypeScript: JSDoc comments (`/** */`)

Only documents public APIs that are missing documentation.
