---
name: prs
description: Read-only status table across open PRs — CI, CodeRabbit, unresolved threads, idle
---

Use the `envoy:prs` skill exactly as written.

Pass any arguments provided by the user to the skill.

This is the read-only view. To act on what it shows, use `/envoy:babysit`.
