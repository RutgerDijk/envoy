---
name: cleanup
description: Remove worktree and feature branch after PR is merged
---

Use the `envoy:cleanup` skill exactly as written.

Supported flags:
- `--all` - Clean up ALL merged worktrees
