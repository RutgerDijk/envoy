---
name: wiki-sync
description: Sync docs/wiki/ to GitHub wiki repository
---

Use the `envoy:wiki-sync` skill exactly as written.

This syncs the `docs/wiki/` directory to the GitHub wiki repository,
keeping documentation in sync with code.
