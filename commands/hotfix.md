---
name: hotfix
description: Fast-path single-defect fix — worktree + failing test + verification + PR, no brainstorm
---

Use the `envoy:hotfix` skill exactly as written.

Pass any arguments provided by the user to the skill. Accepts either a short
`<description>` of the defect or an existing `#issue` number.
