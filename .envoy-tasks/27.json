{
  "$schemaVersion": "1",
  "issueNumber": 27,
  "strategy": "sequential",
  "tasks": [
    {
      "id": "task-1",
      "title": "Version lockstep script (TDD)",
      "description": "Failing tests first for scripts/sync-versions.js check/apply modes over the .version-bump.json manifest list, then the implementation.",
      "files": [
        "scripts/sync-versions.js",
        ".version-bump.json",
        "tests/scripts/sync-versions.test.js"
      ],
      "acceptance": [
        "node scripts/sync-versions.js --check exits 0 when all manifests agree and 1 listing each mismatch otherwise",
        "node scripts/sync-versions.js --apply <version> rewrites every manifest listed in .version-bump.json in place",
        "node tests/scripts/sync-versions.test.js passes"
      ],
      "dependsOn": []
    },
    {
      "id": "task-2",
      "title": "Repair marketplace.json and bump to 2.4.1",
      "description": "Delete the 13 dangling envoy-bundle-* entries (sources point at nonexistent ./bundles/), then --apply 2.4.1 and verify with --check.",
      "files": [
        ".claude-plugin/marketplace.json",
        ".claude-plugin/plugin.json"
      ],
      "acceptance": [
        "marketplace.json contains no envoy-bundle-* entries and no ./bundles/ sources",
        "plugin.json and marketplace.json metadata + envoy entry all read 2.4.1",
        "node scripts/sync-versions.js --check exits 0"
      ],
      "dependsOn": [
        "task-1"
      ]
    },
    {
      "id": "task-3",
      "title": "Document release procedure",
      "description": "Add a Release section to CLAUDE.md: bump via scripts/sync-versions.js --apply, --check must pass, tag v<version> at merge.",
      "files": [
        "CLAUDE.md"
      ],
      "acceptance": [
        "CLAUDE.md documents bump, check, and tag-at-merge steps referencing scripts/sync-versions.js"
      ],
      "dependsOn": [
        "task-2"
      ]
    }
  ]
}
