# Envoy 2.0 — Token-Optimized Workflows with PR-First Review

> **For Claude:** Use envoy:executing-plans to implement this spec task-by-task.

## Overview

Major upgrade to Envoy that reorders the finalize flow (PR first, then parallel reviews), optimizes token usage across all skills, adds persistent learning from reviews, and introduces hook-based automation for polling and batch processing.

**Priority #1: Token optimization without losing quality.**

## Architecture

### PR-First Finalize Flow

Reorder `/envoy:finalize` so the PR is created early, enabling GitHub CodeRabbit to review in parallel with local reviews. All review agents run as parallel subagents. Every finding (including nitpicks) gets addressed. Claude replies to each GitHub CodeRabbit comment and resolves the thread.

```
1. Docstrings + docs (quick wins first)
2. Push branch + gh pr create
3. Parallel subagents:
   ├── Local CodeRabbit CLI
   ├── Sonnet AI review (spec compliance, TDD, codebase patterns)
   └── Visual review (DevTools) + app health check
4. Background: Poll GitHub for CodeRabbit App comments
5. Merge ALL findings from all sources
6. Address every finding (including nitpicks)
7. Reply to each GitHub CR comment with fix + commit hash → resolve thread
8. Push fixes
9. If new CodeRabbit comments appear → address → push (max 3 cycles)
10. Verify:
    - Tests pass
    - Lint clean
    - Build succeeds
    - Application still running (health check)
    - Zero unresolved PR conversations
11. Wiki sync
```

### Token-Optimized AI Review

Replace single Opus reviewer with a single **Sonnet** agent focused on what CodeRabbit can't do:
- Spec/acceptance criteria compliance
- TDD verification (git log: tests committed before implementation?)
- Codebase pattern consistency (explores surrounding code, not just diff)
- Stack profile: load only the "Common Mistakes" section for changed-file stacks

CodeRabbit (zero tokens from us) handles: style, naming, security, common language mistakes, performance anti-patterns.

Visual review (DevTools, no LLM tokens) handles: UI correctness, console errors, network failures.

**Estimated savings: ~60% cost reduction per review cycle.**

### Token Optimization Across All Skills

**Selective stack loading:**
- Only load stack profiles for files that actually changed (`git diff --name-only`)
- Load only the relevant section (Common Mistakes for review, Best Practices for implementation)

**Lean skill descriptions:**
- All skill descriptions under 30 words (triggers only, no workflow summary)
- Audit existing 23 skills and trim

**Batched stop-phase processing:**
- PostToolUse hook accumulates edited file paths to temp file
- Stop hook runs lint/format once across all files (not per-edit)
- Eliminates N formatter outputs from context window

**Complexity-tier pipeline depth:**
- Trivial changes (docs, config, typos): skip AI review entirely, just lint + CodeRabbit
- Small changes (single file logic): Sonnet review only
- Medium/large changes: full parallel review

**allowedTools restrictions per phase:**
- Review subagents: Read, Grep, Glob only (prevents accidental edits)
- Implementation agents: full tool access
- Verification: Read, Grep, Glob, Bash only

**Async hooks for non-critical operations:**
- Learning extraction: async, no context pollution
- Cost tracking: async JSONL logging
- Only blocking hooks: config protection, verification gates

**Dynamic context injection:**
- Mode-specific context fragments (review.md, implement.md, research.md)
- Swap in/out based on current phase instead of loading everything

### Hook System

**New hooks:**

| Hook | Type | Trigger | Action |
|------|------|---------|--------|
| `post-pr-poll` | PostToolCall | `gh pr create` | Start background polling for GitHub CodeRabbit comments |
| `post-edit-accumulator` | PostToolUse | Edit/Write on code files | Append path to temp file for batched processing |
| `stop-batch-lint` | Stop | End of response | Run lint/format once across all accumulated files |
| `config-protection` | PreToolUse | Edit/Write on config files | Block modifications to linter/formatter configs |
| `cost-tracker` | Stop (async) | End of response | Log token usage to JSONL metrics file |
| `learning-extractor` | Stop (async) | End of response (if review session) | Extract patterns from review findings |

**Hook profiles:**
- `minimal` — Only config-protection and cost-tracker
- `standard` — All hooks (default)
- `strict` — All hooks + additional verification gates

Controlled via environment variable: `ENVOY_HOOK_PROFILE=minimal|standard|strict`

### Memory & Continuous Learning

**Post-review learning loop:**
1. After each review cycle, async Stop hook collects all findings
2. Categorize: recurring (seen 2+ times) vs one-off
3. Recurring patterns saved to `memory/review-learnings.md` with:
   - Pattern description
   - Times seen
   - Stack/file type affected
   - Fix template
4. Next review cycle loads learnings FIRST → Sonnet checks for known patterns before external review
5. Patterns that stop appearing after 5 reviews get archived (the team learned)

**Cost tracking:**
- JSONL log at `~/.claude/metrics/envoy-costs.jsonl`
- Per-session: model, tokens, estimated cost, phase (review/implement/brainstorm)
- Enables optimization decisions based on actual usage data

### Verification Loop

After addressing all findings:
1. Run tests (`dotnet test`, `npm test`)
2. Run lint (`npm run lint`)
3. Run build (`dotnet build`, `npm run build`)
4. Health check: verify application is still running (HTTP ping or process check)
5. Check: zero unresolved PR conversations (`gh api` for review threads)
6. Check: no new CodeRabbit comments since last push
7. If new comments appeared → address → push → re-verify (max 3 cycles)
8. After 3 cycles with remaining issues → escalate to user
9. Only claim "ready for human review" with evidence (test output, build output, zero-comments proof)

### Parallelization & Subagent Orchestration

**Review parallelization (finalize):**
- 3 subagents in parallel: Local CR, Sonnet AI review, Visual review
- Background non-blocking poll for GitHub CodeRabbit
- Each subagent gets clean context (no implementation bias)
- Findings merged after all complete

**Execution parallelization (executing-plans):**
- Dependency-aware task graph from spec
- Independent tasks dispatched as parallel subagents in worktrees
- Fresh Sonnet reviewer per task (author never reviews own work)
- Wave-based: run independent tasks in parallel, sync between waves

**Subagent prompt optimization:**
- Pass context via files (spec path, diff path), not inline in prompt
- Restrict tools per phase (read-only for review, full for implementation)
- Complexity-tier: trivial tasks skip review, small tasks get light review

### Config Protection

PreToolUse hook blocks modifications to:
- `.eslintrc*`, `eslint.config.*`
- `.prettierrc*`, `prettier.config.*`
- `biome.json`
- `.editorconfig`
- `tsconfig.json` (warn only, don't block)

Forces Claude to fix source code rather than weaken the linter. This is especially important during autonomous review-fix loops.

## Acceptance Criteria

- [ ] `/envoy:finalize` creates PR before running reviews
- [ ] Local CodeRabbit, Sonnet AI review, and Visual review run as parallel subagents
- [ ] Background hook polls GitHub for CodeRabbit comments every 2-3 minutes
- [ ] Every CodeRabbit finding (including nitpicks) gets addressed
- [ ] Claude replies to each GitHub CodeRabbit PR comment with fix + commit hash
- [ ] Claude resolves each addressed PR conversation thread
- [ ] Verification loop checks: tests, lint, build, app health, zero unresolved comments
- [ ] Max 3 fix-and-verify cycles before escalating to user
- [ ] AI review uses Sonnet model (not Opus) for 60% cost savings
- [ ] AI review focuses on spec compliance, TDD verification, codebase patterns
- [ ] Stack profiles loaded selectively (only changed-file stacks, relevant sections only)
- [ ] All skill descriptions audited to under 30 words
- [ ] PostToolUse accumulator batches lint/format to Stop phase
- [ ] Config protection hook blocks linter config modifications
- [ ] Cost tracker logs to JSONL per session
- [ ] Learning extractor saves recurring review patterns to memory
- [ ] Next review cycle checks known patterns before external review
- [ ] Hook profiles (minimal/standard/strict) controlled via env var
- [ ] Complexity tiers: trivial changes skip AI review
- [ ] Subagent tools restricted per phase
- [ ] Application health check in verification step

---

## Implementation Plan

**Execution Strategy:** `batch`

Batches ordered by dependency:

```yaml
execution:
  strategy: batch
  batches:
    - name: "Foundation — Hooks Infrastructure"
      tasks: [1, 2, 3, 4]
    - name: "Core — Finalize Flow Reorder"
      tasks: [5, 6, 7]
    - name: "Review — Token-Optimized AI Review"
      tasks: [8, 9, 10]
    - name: "Automation — Polling & Learning"
      tasks: [11, 12, 13]
    - name: "Polish — Verification & Optimization"
      tasks: [14, 15, 16, 17]
```

### Task 1: Add PostToolUse edit accumulator hook

**Files:**
- Create: `hooks/post-edit-accumulator.js`
- Modify: `hooks/hooks.json`

**Steps:**
1. Create `hooks/post-edit-accumulator.js` that:
   - Reads stdin for tool call info (file path)
   - Filters for JS/TS/CS/CSX files only
   - Appends edited file path to `/tmp/envoy-edited-${sessionId}.txt`
   - Exports `run(rawInput)` function for in-process execution (no child process spawn)
   - Exits 0 always (non-blocking)
2. Register in `hooks/hooks.json` as PostToolUse hook on Edit/Write
3. Commit: `git commit -m "feat(hooks): add post-edit accumulator for batched processing"`

### Task 2: Add stop-phase batch lint hook

**Files:**
- Create: `hooks/stop-batch-lint.js`
- Modify: `hooks/hooks.json`

**Steps:**
1. Create `hooks/stop-batch-lint.js` that:
   - Reads accumulated file paths from `/tmp/envoy-edited-${sessionId}.txt`
   - Deduplicates paths
   - Runs lint/format once across all files
   - Deletes the temp file after processing
   - Exports `run(rawInput)` for in-process execution
2. Register in `hooks/hooks.json` as Stop hook
3. Commit: `git commit -m "feat(hooks): add stop-phase batch lint for token savings"`

### Task 3: Add config protection hook

**Files:**
- Create: `hooks/config-protection.js`
- Modify: `hooks/hooks.json`

**Steps:**
1. Create `hooks/config-protection.js` that:
   - Checks if target file matches protected list (eslintrc, prettierrc, biome.json, etc.)
   - Returns exit code 2 with message: "BLOCKED: Fix source code instead of weakening linter config"
   - Exports `run(rawInput)` for in-process execution
2. Register in `hooks/hooks.json` as PreToolUse hook on Edit/Write
3. Commit: `git commit -m "feat(hooks): add config protection to prevent linter gaming"`

### Task 4: Add hook profile system

**Files:**
- Create: `hooks/hook-runner.js` (profile-aware runner)
- Modify: `hooks/hooks.json` (add profile annotations)

**Steps:**
1. Create `hooks/hook-runner.js` that:
   - Reads `ENVOY_HOOK_PROFILE` env var (default: `standard`)
   - Each hook entry gets a `profiles` array: `["minimal", "standard", "strict"]`
   - Runner skips hooks not in current profile
   - Supports `ENVOY_DISABLED_HOOKS` for per-hook overrides
2. Annotate all hooks in `hooks.json` with profile membership
3. Commit: `git commit -m "feat(hooks): add profile system (minimal/standard/strict)"`

### Task 5: Reorder finishing-branch skill — PR first

**Files:**
- Modify: `skills/finishing-branch/SKILL.md`

**Steps:**
1. Reorder the process:
   - Step 1: Docstrings + docs (quick wins)
   - Step 2: Push branch + `gh pr create`
   - Step 3: Dispatch parallel review subagents
   - Step 4-10: Merge findings, address, verify loop
   - Step 11: Wiki sync
2. Add PR creation before review (currently after)
3. Add complexity-tier detection (trivial/small/medium/large based on diff size + file types)
4. Commit: `git commit -m "feat(skills): reorder finalize — PR first, then parallel reviews"`

### Task 6: Add GitHub CodeRabbit comment parsing skill

**Files:**
- Create: `skills/coderabbit-pr-review/SKILL.md`

**Steps:**
1. Create skill that:
   - Polls `gh api repos/{owner}/{repo}/pulls/{pr}/comments` for CodeRabbit comments
   - Parses each comment: file, line, severity, suggestion
   - For each finding: apply fix, commit, reply to comment with fix + commit hash
   - Resolve the conversation thread via `gh api`
   - Return list of all addressed findings
2. Include instructions for handling nitpicks (address all, no skipping)
3. Commit: `git commit -m "feat(skills): add CodeRabbit PR comment parsing and resolution"`

### Task 7: Add post-PR poll hook

**Files:**
- Create: `hooks/post-pr-poll.sh`
- Modify: `hooks/hooks.json`

**Steps:**
1. Create hook that triggers after `gh pr create`:
   - Extracts PR number from command output
   - Saves PR number to `/tmp/envoy-active-pr.txt`
   - Outputs instruction for Claude to poll for CodeRabbit comments
2. Register as PostToolCall hook matching `gh pr create`
3. Commit: `git commit -m "feat(hooks): add post-PR CodeRabbit poll trigger"`

### Task 8: Modify layered-review for token-optimized AI review

**Files:**
- Modify: `skills/layered-review/SKILL.md`

**Steps:**
1. Change Layer 2 (AI Review):
   - Use `model: "sonnet"` for review subagent
   - Focus scope: spec compliance, TDD verification, codebase pattern consistency
   - Remove overlap with CodeRabbit (style, security, common mistakes)
   - Restrict tools: `Read, Grep, Glob` only (read-only review)
2. Add selective stack loading: only load "Common Mistakes" section for changed-file stacks
3. Add complexity-tier logic:
   - Trivial (docs/config only): skip AI review, just lint + CodeRabbit
   - Small (1-3 files, no logic): Sonnet review only
   - Medium/large: full review
4. Commit: `git commit -m "feat(skills): token-optimized AI review with Sonnet + selective loading"`

### Task 9: Audit and trim all skill descriptions

**Files:**
- Modify: All 23 `skills/*/SKILL.md` frontmatter descriptions

**Steps:**
1. Read each skill's description
2. Trim to under 30 words, keeping only trigger conditions
3. Remove any workflow summaries from descriptions
4. Verify each description still starts with "Use when..."
5. Commit: `git commit -m "chore(skills): trim all descriptions to <30 words for token savings"`

### Task 10: Add selective stack profile loading to lib

**Files:**
- Modify: `lib/stack-loader.js`

**Steps:**
1. Add `loadStackSection(stackName, section)` function that loads only a specific section (e.g., "Common Mistakes", "Best Practices", "Review Checklist")
2. Add `detectStacksFromDiff(baseBranch)` that only detects stacks for changed files
3. Update existing `loadStackProfiles()` to accept optional section filter
4. Commit: `git commit -m "feat(lib): add selective stack profile section loading"`

### Task 11: Add cost tracker hook

**Files:**
- Create: `hooks/cost-tracker.js`
- Modify: `hooks/hooks.json`

**Steps:**
1. Create async Stop hook that:
   - Reads session info from stdin (model, tokens)
   - Appends JSONL record to `~/.claude/metrics/envoy-costs.jsonl`
   - Includes: timestamp, session_id, model, input_tokens, output_tokens, phase
   - Exports `run(rawInput)` for in-process execution
2. Register as async Stop hook (non-blocking, no context pollution)
3. Commit: `git commit -m "feat(hooks): add async cost tracker for optimization insights"`

### Task 12: Add learning extractor hook

**Files:**
- Create: `hooks/learning-extractor.js`
- Modify: `hooks/hooks.json`

**Steps:**
1. Create async Stop hook that:
   - Detects if session included a review (check for review-related tool calls)
   - Collects all findings from the session
   - Compares against existing `memory/review-learnings.md`
   - If pattern seen 2+ times: add/update entry with count, stack, fix template
   - If pattern not seen in 5+ reviews: archive it
2. Register as async Stop hook
3. Commit: `git commit -m "feat(hooks): add learning extractor for continuous improvement"`

### Task 13: Add pre-review learning check to layered-review

**Files:**
- Modify: `skills/layered-review/SKILL.md`

**Steps:**
1. Add step before Layer 2: "Load known patterns from memory/review-learnings.md"
2. Sonnet reviewer checks for known patterns first (cheap local check before external review)
3. Known patterns flagged immediately without waiting for CodeRabbit
4. Commit: `git commit -m "feat(skills): load review learnings before AI review"`

### Task 14: Add verification loop with app health check

**Files:**
- Modify: `skills/verification/SKILL.md`

**Steps:**
1. Add to verification checklist:
   - Application health check (HTTP ping to localhost or process check)
   - Zero unresolved PR conversations check via `gh api`
   - No new CodeRabbit comments since last push
2. Add max 3 fix-and-verify cycles
3. Add escalation to user after 3 cycles
4. Add "evidence before assertions" requirement for each check
5. Commit: `git commit -m "feat(skills): add app health check and PR conversation verification"`

### Task 15: Update executing-plans for smarter parallelization

**Files:**
- Modify: `skills/executing-plans/SKILL.md`

**Steps:**
1. Add complexity-tier pipeline depth:
   - Trivial: implement → test
   - Small: implement → test → light review
   - Medium: implement → test → full review
   - Large: research → implement → test → full review
2. Add allowedTools restrictions per phase
3. Add fresh Sonnet reviewer per task (not implementing agent)
4. Commit: `git commit -m "feat(skills): complexity-tier parallelization with tool restrictions"`

### Task 16: Add dynamic context fragments

**Files:**
- Create: `contexts/review.md`
- Create: `contexts/implement.md`
- Create: `contexts/research.md`

**Steps:**
1. Create minimal context fragments (~15-20 lines each):
   - `review.md`: focus on finding issues, read-only tools preferred, output format for findings
   - `implement.md`: focus on TDD cycle, full tool access, commit after each task
   - `research.md`: focus on exploration, read-only, summarize findings
2. Update relevant skills to reference context fragments
3. Commit: `git commit -m "feat(contexts): add phase-specific context fragments for token efficiency"`

### Task 17: Update README and CLAUDE.md for 2.0

**Files:**
- Modify: `README.md`
- Modify: `CLAUDE.md`

**Steps:**
1. Update README:
   - Document new finalize flow (PR first)
   - Document hook system and profiles
   - Document token optimization features
   - Update architecture diagram
   - Update command table
2. Update CLAUDE.md:
   - Add hooks conventions section
   - Add context fragments documentation
   - Document hook profile system
3. Commit: `git commit -m "docs: update README and CLAUDE.md for Envoy 2.0"`

---

*Generated by Envoy*
